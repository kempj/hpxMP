#Automatically generated by configure
#compilers and linkers

#config name
LABEL=clang


#compilers
CC=clang

#OMPC=clang -fopenmp  -emit-llvm -S -c 
#OMPLINK=clang -fopenmp -g -emit-llvm -S -c
#CLINK=clang -g -emit-llvm -S -c

OMPC=clang -fopenmp -g
OMPLINK=clang -fopenmp -g 
CLINK=clang -g

#compiler and linker flags

OPT_FLAGS=-O0 -g

CC_FLAGS=
OMPC_FLAGS=

CLINK_FLAGS=
OMPLINK_FLAGS=

